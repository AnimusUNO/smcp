[phases.setup]
nixPkgs = ["python3"]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install -r requirements.txt"
]

[start]
cmd = "python3 smcp.py --allow-external --host 0.0.0.0"

